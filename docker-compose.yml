services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: mindbalance
      POSTGRES_USER: mindbalance
      POSTGRES_PASSWORD: mindbalance
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mindbalance"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      DATABASE_URL: postgresql://mindbalance:mindbalance@db:5432/mindbalance
    depends_on:
      db:
        condition: service_healthy
    # entrypoint.sh runs: alembic upgrade head && uvicorn
    # Do NOT add 'command:' here — it would bypass entrypoint.sh

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
    depends_on:
      - backend

volumes:
  postgres_data:
